program test_program is
    int fibb_result;
    procedure fibb(int n in, int result out)
        int a;
        int b;
    begin
        if (i < 0) then
            result := -1;
            return;
        end if;
        if (i == 0) then
            result := 0;
            return;
        end if;
        if (i == 1) then
            result := 1;
            return;
        end if;
        
        fibb(n-1, a);
        fibb(n-2, b);
        
        result := a + b;
    end procedure;
begin
    fibb(5, fibb_result);
    putInt(fibb_result);
end program